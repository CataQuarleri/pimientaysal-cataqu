<!DOCTYPE html>
<html>
<!-- head -->
<%- include('./partials/head') %>

<body>
  <!-- Navbar (sit on top) -->
  <%- include('./partials/navbar') %>

  <!-- Header -->
  <%- include('./partials/header') %>

  <!-- Page content -->
  <div class="ps-content">

    <!-- Menu Section -->
<!-- Plato seleccionado -->
    <div class="ps-row ps-padding-64" id="menu">
      <% let id = query; %>
      <% if (platos[id].description == undefined) { %>
      <div class="ps-col l6 ps-padding-large">
        <h1 class="ps-center"><%= platos[id].name %> </h1>
        <br />
        <h4><%= 'Plato típico' %> </h4>
        <p class="ps-text-grey">
          <%= platos[id].price %>
        </p>
      </div>
      <% } else { %>
      <div class="ps-col l6 ps-padding-large">
        <h1 class="ps-center"> <%= platos[id].name %></h1>
        <br />
        <h4><%= platos[id].description %></h4>
        <p class="ps-text-grey">
          <%= platos[id].price %>
        </p>
      </div>
      <% } %>

      <!-- Imagen del plato seleccionado -->
      <% if (platos[id].pic == undefined) { %>
      <div class="ps-col l6 ps-padding-large ps-img-menu">
        <img src="/images/menu.jpg" class="ps-round ps-image ps-opacity-min" alt='Menu' />
      </div>
      <% } else { %>
      <div class="ps-col l6 ps-padding-large ps-img-menu">
        <img src=<%= platos[id].pic%> class="ps-round ps-image ps-opacity-min" alt=<%= platos[id].name %> />
      </div>
      <% } %>



    </div>

    <!-- Footer -->
    <%- include('./partials/footer') %>
  </div>
</body>

</html>